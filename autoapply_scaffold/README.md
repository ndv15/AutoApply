# AutoApply (Hybrid: Auto where allowed, Assist elsewhere)

Quickstart:
```
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
uvicorn app.api:app --reload --port 8787
# open http://localhost:8787
```
